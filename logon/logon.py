import subprocess
import tkinter as tk

root = tk.Tk()



#path = '\\ESAUSRV01.unterrichtsnetz.local\NETLOGON\logon\packages-lehrer\packages.config'

# To also capture stderr...
#result = subprocess.run(['choco', 'install', path, '-y'], stdout=subprocess.PIPE, stderr=subprocess.PIPE, universal_newlines=True)



#out = tk.Label(root, text=result.stdout)
#err = tk.Label(root, text=result.stderr)
#out.pack()
#err.pack()


root.mainloop()


#print("HELLO WORLD")
#input("Press Enter to continue...")